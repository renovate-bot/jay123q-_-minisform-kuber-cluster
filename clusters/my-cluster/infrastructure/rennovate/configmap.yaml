apiVersion: v1
kind: ConfigMap
metadata:
  name: renovate-config
  namespace: rennovate
data:
  config.json: |-
    {
      "$schema": "https://docs.renovatebot.com/renovate-schema.json",
      "dryRun": "full"
    }
