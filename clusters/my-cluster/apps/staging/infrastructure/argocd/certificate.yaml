apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-staging-tls-cert
  namespace: istio-system
spec:
  secretName: argocd-staging-tls-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - argocd-staging.yourdomain.com
